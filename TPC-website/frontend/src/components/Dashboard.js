import React from 'react';

const Dashboard = () => {
    return (
        <div>
            <h2>Admin Dashboard</h2>
            {/* Implement admin functionalities like managing students, companies, jobs */}
        </div>
    );
};

export default Dashboard;
